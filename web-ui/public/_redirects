# Cloudflare Pages redirects/rewrites

# Rewrite /install to serve the static install page (200 = internal rewrite)
/install /install.html 200

# Redirect old /descargar paths to /install
/descargar /install 301
/descargar/ /install 301